import ProtectedLayout from "../../components/protected_layout";

const Dashboard = () => {
  return <ProtectedLayout>
    <div className="flex items-center">Currently under development!</div>
  </ProtectedLayout>;
};

export default Dashboard;
